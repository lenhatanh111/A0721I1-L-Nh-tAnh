let inputwidth;
let inputheight;
intputwidth=prompt("Enter the width ");
intputheight=prompt("Enter the height ");
let width =parseInt(intputwidth);
let height =parseInt(intputheight);
let area=width*height;
document.write("the area is:"+area)